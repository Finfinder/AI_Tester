[pytest]
pythonpath = .
addopts = -m "not openrouter_smoke"
testpaths =
    orchestrator
    agents
    schemas
markers =
    openrouter_smoke: Requires OPENROUTER_API_KEY and calls the real OpenRouter API.
